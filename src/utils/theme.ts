import type { GlobalTheme } from "naive-ui";
export const changeTheme = (mode:string,theme:GlobalTheme | null)=>{
    switch (mode) {
        case 'light':
            theme
            break;
    
        default:
            break;
    }
}